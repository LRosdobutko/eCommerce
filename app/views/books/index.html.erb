<h1>Books</h1>
<ul>
  <% @books.each do |book| %>
    <%= render 'book', book: book %>
  <% end %>
</ul>

<div class="pagination">
  <%= paginate @books %> <!-- This will create pagination links -->
</div>

